<%= @topic.topic %>

<h2> Mentors In This Topic </h2>

<%= form_for @pairing do |f| %>
<div class="field">
  <%= f.label :mentor %>
  <%= f.collection_select :mentor_id, @mentors, :id, :name_bio %>
</div>

<div class="field">
  <%= f.submit %>
</div>
<% end %>
