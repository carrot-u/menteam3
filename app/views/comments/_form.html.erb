<%= form_for [@post, comment] do |f| %>
    
<p>
    <div class = "field">
        <%= f.label :comment, "Add a Comment" %>
        <br>
        <%= f.text_area :comment, size: "40x10" %>
    </div>

    <div class="actions">
        <%=f.submit 'Post your Comment', :class => "btn font-weight-bold border-secondary btnpairing" %>
 <% end %>
 </p>