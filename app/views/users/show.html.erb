<!DOCTYPE html>
<html>

<head>
</head>
<body>

<h1>Welcome to your profile!!!</h1>

<p><strong>First Name:</strong> <%= @user.first_name%></p>
<p><strong>Last Name:</strong> <%= @user.last_name %></p>
<p><strong>Email:</strong> <%=@user.email %></p>
<p><strong>Position:</strong> <%= @user.position %></p>
<p><strong>Bio:</strong> <%= @user.bio %></p>

<%= render partial: 'user_tags/form'%>
<h2> Mentoring for the following topics:</h2>
<% @user_tags_list.each do |user_selection| %>

  <br><%= topic_name = Topic.find(user_selection.topic_id).topic %></br>

<% end %>

<%= link_to "View your mentors and mentees", pairings_path(:id => @user.id)%></br>

<% request = 'Request' %>
<% request += "(#{@pending_mentor_request.length})" if @pending_mentor_request.any? %>
<%= link_to request, pairing_path(session[:user_id]) %></br>
<%= link_to "Search for Topics", topics_path %></br>
<%= link_to "Become a Mentor", new_user_tag_path %></br>
<%= link_to 'Inbox', posts_path%>
<%#<%= link_to "Log out", logout_path, method: :delete %>
<div>
<%= link_to 'Edit Profile', edit_user_path %>
</div>

<%= render 'form', user: @user %>

<h1>Mentoring For</h1>
<div class = "col-md-8 offset-2">
  <% @users.each do |user| %>
    <p><small>Topics: <%= raw user.topics.map(&:topic).map { |t| link_to t, tag_path(t) }.join(', ') %></small</p>
    <p><%= link_to "Profile", users_path(user) %></p>
  <% end %>
</div>



</body>
</html>
