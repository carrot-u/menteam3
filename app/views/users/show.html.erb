<!DOCTYPE html>
<html>

<head>
</head>
<body>

<h1 class= "text-center welcome" >Welcome to your profile</h1>

<p><strong>First Name:</strong> <%= @user.first_name%></p>
<p><strong>Last Name:</strong> <%= @user.last_name %></p>
<p><strong>Email:</strong> <%=@user.email %></p>
<p><strong>Position:</strong> <%= @user.position %></p>
<p><strong>Bio:</strong> <%= @user.bio %></p>

<%= render partial: 'user_tags/form'%>
<h2 class="title"> Mentoring for the following topics:</h2>
<% @user_tags_list.each do |user_selection| %>

  <li><%= topic_name = Topic.find(user_selection.topic_id).topic %></li>

<% end %>

<% request = 'Request' %>
<% request += "(#{@pending_mentor_request.length})" if @pending_mentor_request.any? %>
<%= link_to request, pairing_path(session[:user_id]) %></br>
<%= link_to 'Inbox', posts_path%>
<%#<%= link_to "Log out", logout_path, method: :delete %>
<div>
<%= link_to 'Edit Profile', edit_user_path %>
</div>



</body>
</html>
